# TeamMind 当前系统状态

最后更新：2026-05-18

## 1. 当前阶段

TeamMind 已从 Milestone 1 memory foundation 进入 Phase 3 Alpha Wave 0 收尾阶段。当前目标不是继续堆单点 demo 能力，而是为后续 Wave 建立稳定施工系统：

```text
开发规则
-> issue spec
-> 核心数据合同
-> smoke baseline
-> 当前状态与 handoff
```

当前产品方向仍然是公司级上下文、记忆、项目推进和行动回流系统，不是聊天界面、prompt 模板工具或自动外部执行 agent。

## 2. 当前稳定基线

当前代码基线：

```text
main @ f218279
```

本轮本地集成分支：

```text
integration/phase-3-wave-00
```

阶段目标集成分支：

```text
integration/phase-3-alpha
```

旧 memory foundation 分支仍保留为历史参考，但它已经不再是后续开发的主要 base：

```text
integration/CTX-01-MEM-01-MEM-03-REC-01-CTX-03-MEM-02-memory-foundation
```

## 3. 已完成能力

当前代码已经具备以下基础能力：

- 项目空间和 demo 数据。
- 上下文录入。
- 从上下文生成公司 memory。
- memory status 基础能力。
- memory 来源引用基础能力。
- memory 详情面板。
- memory 编辑和状态流转。
- extract memories pipeline。
- reconcile memories pipeline。
- action 生成。
- action 的 `whyNow`、`evidenceMemoryIds`、`expectedArtifact` 兼容字段。
- brief 生成。
- brief 的 `evidenceMemoryIds` 和 `sourceContextIds`。
- result feedback 基础回流。
- result 的 `whatChanged`、`newEvidence`、`followUpNeeded` 和 memory update 结构。
- 本地 smoke test。
- 静态本地运行。

Wave 0 已完成或正在收尾：

- `DEV-00`：新增 Wave 0 issue spec，明确执行顺序。
- `ARCH-00`：定义 Phase 3 Alpha 核心数据合同。
- `QA-00`：增强 smoke test baseline。
- `DOC-00`：更新当前系统状态。
- `DOC-01`：更新 AI handoff。

## 4. 当前仍偏 demo 的部分

以下能力已经有形状或合同，但还不是完整 Alpha 实现：

- Inbox 还不是统一信息入口。
- Source / Signal / Memory 分层已经定义合同，但 Wave 1 才会正式落地。
- Entity Profile 尚未实现。
- Project Node 尚未实现。
- Action Brief 仍需要按场景结构化。
- Result Feedback 已能产生 memory update 结构，但还没有完整 review flow。
- Command Center 还没有成为公司级首页。
- AgentRun、schema 校验、repository abstraction 仍未完整落地。
- AI provider 和外部 integration 仍是未来方向，不属于当前优先级。

## 5. 当前文档体系

长期愿景：

- `最终产品形态.md`

开发规则：

- `docs/AI_DEVELOPMENT_GUIDE.md`

下一阶段计划：

- `docs/PHASE3_ALPHA_DEVELOPMENT_PLAN.md`

接手入口：

- `AI_HANDOFF.md`
- `当前系统状态.md`

架构和数据：

- `PROJECT_FUNCTION_STRUCTURE.md`
- `DATA_MODEL.md`

issue 规则：

- `docs/issues/README.md`
- `docs/issues/*.md`

## 6. 下一阶段建议

Wave 0 完成后，从 Wave 1 开始：

```text
INBOX-01-manual-source-inbox
PIPE-01-source-to-signal
LINK-01-project-entity-suggestion
UI-01-inbox-review-flow
QA-01-inbox-smoke-flow
```

Wave 1 的核心边界：

- 支持手动输入真实业务信息。
- 生成 `Source`。
- 从 `Source` 抽取 `Signal`。
- 建议关联 Entity / Project。
- 支持 review flow。
- 不接 Gmail / Slack API。
- 不自动执行外部动作。

## 7. 当前风险

- `src/main.js`、`src/ui/render.js`、`src/domain/agentEngine.js` 仍是高冲突文件，Wave 1 要小步推进。
- `Source` / `Signal` 与旧 `ContextItem` 需要兼容迁移，不能导致旧 localStorage 白屏。
- 数据合同已经扩展，后续实现必须同步 demo、smoke test 和文件职责文档。
- Command Center 相关对象已定义合同，但不能提前做成营销 landing page 或泛聊天入口。
- 外部集成不应早于手动信息闭环。

## 8. 验证命令

每个 issue 完成后运行：

```bash
node scripts/smoke-test.mjs
```

当前 Wave 0 已在 DEV-00、ARCH-00、QA-00 及其合并后分别通过该命令。

涉及 UI 的改动建议本地预览：

```bash
npm run dev
```

浏览器打开：

```text
http://127.0.0.1:4173
```
