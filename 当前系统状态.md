# TeamMind 当前系统状态

最后更新：2026-05-19

## 1. 当前阶段

TeamMind 已完成 Phase 3 Alpha Wave 2：Entity Profile + Project Node。当前系统已经具备手动信息进入、Source 保存、Signal 提取、Entity / Project 建议关联、Entity Profile、Project Node、Node Detail，以及人工 review 后转 Memory / Action / Node 证据链的第一版闭环。

```text
手动信息录入
-> Source
-> Signal
-> Entity / Project 建议
-> Entity Profile / Project Node 证据链
-> 人工 review
-> Memory / Action 草稿
-> Result 回流到 Project Node
```

当前产品方向仍然是公司级上下文、记忆、项目推进和行动回流系统，不是聊天界面、prompt 模板工具或自动外部执行 agent。

## 2. 当前稳定基线

当前代码基线：

```text
main @ f516ed4
```

本轮本地集成分支：

```text
integration/phase-3-wave-02
```

阶段目标集成分支：

```text
integration/phase-3-alpha
```

旧 memory foundation 分支仍保留为历史参考，但它已经不再是后续开发的主要 base：

```text
integration/CTX-01-MEM-01-MEM-03-REC-01-CTX-03-MEM-02-memory-foundation
```

## 3. 已完成能力

当前代码已经具备以下基础能力：

- 项目空间和 demo 数据。
- 上下文录入。
- 从上下文生成公司 memory。
- memory status 基础能力。
- memory 来源引用基础能力。
- memory 详情面板。
- memory 编辑和状态流转。
- extract memories pipeline。
- reconcile memories pipeline。
- action 生成。
- action 的 `whyNow`、`evidenceMemoryIds`、`expectedArtifact` 兼容字段。
- brief 生成。
- brief 的 `evidenceMemoryIds` 和 `sourceContextIds`。
- result feedback 基础回流。
- result 的 `whatChanged`、`newEvidence`、`followUpNeeded` 和 memory update 结构。
- 手动 Source Inbox。
- `Source` / `Signal` 本地持久化和旧数据兼容迁移。
- `extractSignals()` 本地规则 pipeline。
- `linkSignals()` 本地建议关联 pipeline。
- Signal review flow：确认、忽略、转 Memory、转 Action。
- Memory source references 支持追溯到 `Source` 和 `Signal`。
- Entity Profile 面板，支持对象状态治理和关联 Source / Signal / Memory / Project / Action。
- Entity linking 会在 Signal 建议、转 Memory、转 Action 后持续维护画像证据链。
- Project Node 最小数据结构、默认单节点和状态治理。
- Node Detail Panel，展示节点目标、成功标准、输入上下文、证据链、相关 action 和 result。
- Project Node 会随 Source / Signal / Memory / Action / Result flow 自动维护关联 ID。
- 本地 smoke test。
- 静态本地运行。

Wave 0 已完成并合入 `main`：

- `DEV-00`：新增 Wave 0 issue spec，明确执行顺序。
- `ARCH-00`：定义 Phase 3 Alpha 核心数据合同。
- `QA-00`：增强 smoke test baseline。
- `DOC-00`：更新当前系统状态。
- `DOC-01`：更新 AI handoff。

Wave 1 已完成：

- `INBOX-01`：新增手动 Source Inbox，支持标题、正文、来源、发生时间、参与对象和标签。
- `PIPE-01`：新增 Source -> Signal pipeline，并将 Source 标记为 processed。
- `LINK-01`：为 Signal 建议关联 Entity / Project，并回写 Source 关联建议。
- `UI-01`：新增 Inbox review flow，支持确认、忽略、转 Memory、转 Action。
- `QA-01`：扩展 smoke test 覆盖完整 Inbox flow 和 UI 渲染断言。

Wave 2 已完成：

- `ENTITY-01`：新增 Entity Profile 面板、详情展示、状态治理和 Entity 字段兼容迁移。
- `ENTITY-02`：让 Signal 建议、转 Memory、转 Action 持续回写 Entity 的 Source / Signal / Memory / Project / Action 链接。
- `PROJECT-01`：新增 Project Node 数据结构、默认单节点、节点列表和状态治理。
- `PROJECT-02`：新增 Node Detail Panel，展示目标、成功标准、输入上下文、证据链、action 和 result。
- `QA-02`：扩展 smoke test 覆盖 Source -> Signal -> Entity -> Memory / Action -> Project Node -> Result 的贯通路径。

## 4. 当前仍偏 demo 的部分

以下能力已经有形状或合同，但还不是完整 Alpha 实现：

- Inbox 已有手动入口，但还没有 Gmail / Slack 等外部 intake，也不应在当前阶段自动接入。
- Source / Signal / Memory 分层已经落地第一版，但仍是本地规则 pipeline，不是真实 AI provider。
- Entity Profile 已实现第一版，但复杂 EntityRelation 图谱、实体合并和完整编辑表单尚未实现。
- Project Node 已实现第一版，但 AI 自动拆分多节点、节点编辑表单和节点 agent 尚未实现。
- Action Brief 仍需要按场景结构化。
- Result Feedback 已能产生 memory update 结构，但还没有完整 review flow。
- Command Center 还没有成为公司级首页。
- AgentRun、schema 校验、repository abstraction 仍未完整落地。
- AI provider 和外部 integration 仍是未来方向，不属于当前优先级。

## 5. 当前文档体系

长期愿景：

- `最终产品形态.md`

开发规则：

- `docs/AI_DEVELOPMENT_GUIDE.md`

下一阶段计划：

- `docs/PHASE3_ALPHA_DEVELOPMENT_PLAN.md`

接手入口：

- `AI_HANDOFF.md`
- `当前系统状态.md`

架构和数据：

- `PROJECT_FUNCTION_STRUCTURE.md`
- `DATA_MODEL.md`

issue 规则：

- `docs/issues/README.md`
- `docs/issues/*.md`

## 6. 下一阶段建议

Wave 2 完成后，从 Wave 3 开始：

```text
MEM-05-memory-governance-live
ACTION-01-brief-generation
ACTION-02-result-feedback
REC-03-result-to-memory-update
QA-03-action-loop-smoke
```

Wave 3 的核心边界：

- 让 memory 状态真正影响展示、行动建议和优先级。
- 让 Action Brief 按 customer / investor / coding / operating 等场景结构化。
- 完整化 Result Feedback review flow 和 memory update 建议处理。
- 让 result 影响后续 action、project/node 状态和下一步建议。
- 不接 Gmail / Slack API。
- 不自动执行外部动作。

## 7. 当前风险

- `src/main.js`、`src/ui/render.js`、`src/domain/agentEngine.js` 仍是高冲突文件，Wave 3 继续小步推进。
- `Source` / `Signal` 与旧 `ContextItem` 的兼容迁移已经建立，但后续新增字段仍要避免旧 localStorage 白屏。
- 数据合同已经扩展，后续实现必须同步 demo、smoke test 和文件职责文档。
- Command Center 相关对象已定义合同，但不能提前做成营销 landing page 或泛聊天入口。
- 外部集成不应早于手动信息闭环。

## 8. 验证命令

每个 issue 完成后运行：

```bash
node scripts/smoke-test.mjs
```

当前 Wave 2 已在每个 issue 完成后和每次合回 `integration/phase-3-wave-02` 后分别通过该命令。最新 smoke summary 覆盖 contexts、sources、signals、entities、nodes、memories、actions、briefs、results、Entity/Profile linkage、Project Node detail 和 Source/Signal review flow。

涉及 UI 的改动建议本地预览：

```bash
npm run dev
```

浏览器打开：

```text
http://127.0.0.1:4173
```
